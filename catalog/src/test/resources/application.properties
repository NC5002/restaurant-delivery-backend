# Configuración de base de datos para el entorno de PRUEBA
# Usa una base de datos en memoria (H2) para que las pruebas no dependan de Docker.
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Esta propiedad es ideal para pruebas: crea el esquema, corre la prueba, y lo borra al final.
spring.jpa.hibernate.ddl-auto=create-drop